[workspace]
members = [
    "ledger-generic",
    "ledger-transport",
    "ledger-transport-hid",
]

exclude = [
  "examples/wasm"
]

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"
